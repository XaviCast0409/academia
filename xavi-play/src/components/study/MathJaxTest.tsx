import React from 'react';
import { View } from 'react-native';

// No-op placeholder: MathJax removed from the app. Keep a minimal component to avoid import errors elsewhere.
const MathJaxTest: React.FC = () => {
  return <View />;
};

export default MathJaxTest;
